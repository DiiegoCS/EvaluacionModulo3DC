<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.diegocampos.evaluacionmodulo3dc">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EvaluacionModulo3DC">
        <activity
            android:name=".View.actividades.ConsultaRegistroEquipo"
            android:exported="false" />
        <activity
            android:name=".View.actividades.OtraActividad"
            android:exported="false" />
        <activity
            android:name=".View.actividades.Registro"
            android:exported="false" />
        <activity
            android:name=".View.actividades.Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>